/* ============================================================
   config.js — configuração opcional do banco

   ATENÇÃO: este repositório é PÚBLICO. Não coloque token aqui.

   O endereço e o token do banco são preenchidos DENTRO do app,
   em Avaliações registradas → Banco de dados compartilhado, e
   ficam guardados só no navegador de cada pessoa.

   Os campos abaixo só fazem sentido se um dia o repositório virar
   privado e você quiser deixar tudo já configurado para o time.
   ============================================================ */

const CONFIG = {
  API_URL: '',
  TOKEN: '',
};
