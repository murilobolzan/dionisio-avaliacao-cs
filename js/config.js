/* ============================================================
   config.js — a única coisa que você precisa preencher

   API_URL é o endereço do banco de dados (a planilha publicada
   pelo apps_script.js). Enquanto estiver vazio, o app funciona
   normalmente, mas só no navegador de quem está usando.

   Para ligar o banco compartilhado:
     1. siga o passo a passo no topo de apps_script.js
     2. cole aqui a URL que termina em /exec
     3. salve e publique (git push)
   ============================================================ */

const CONFIG = {
  API_URL: '',
};
